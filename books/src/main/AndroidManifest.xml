<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.game.books">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_books">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>
        <dist:fusing dist:include="false" />
    </dist:module>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name="com.game.books.BooksActivity"
            android:screenOrientation="portrait">

            <!--
            <intent-filter android:order="2" android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="bfinder-instant.com"
                    android:pathPrefix="/books"
                    android:scheme="https" />
            </intent-filter>
            <intent-filter android:order="2" android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="bfinder-instant.com"
                    android:pathPrefix="/books"
                    android:scheme="http" />
            </intent-filter>
            -->

        </activity>
    </application>
</manifest>

