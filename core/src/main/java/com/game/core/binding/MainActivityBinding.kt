package com.game.core.binding
/*
import android.widget.TextView
import androidx.databinding.BindingAdapter
import com.game.core.status.InstallModuleStatus


@BindingAdapter("moduleInstallStatus")
fun setModuleInstallStatus(textView: TextView, status: InstallModuleStatus) {
    when (status) {
        InstallModuleStatus.LOADING_MODULE -> {
            textView.text = "Loading Module"
        }
        InstallModuleStatus.DOWNLOADING -> {
            textView.text = "Downloading"
        }
        InstallModuleStatus.ALREADY_INSTALLED -> {
            textView.text = "Already installed"
        }
        else -> textView.text = "--"
    }
}*/